# Configuración para Vercel con Backend HTTP

# Producción: Usar proxy de Vercel (URL relativa)
VITE_URL_API=

# Esta configuración hace que:
# - En desarrollo (localhost): use HTTP directo al backend
# - En producción (Vercel): use URLs relativas que Vercel proxea al backend HTTP
